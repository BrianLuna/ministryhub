/// Error codes mapped to localization keys for authentication failures.
abstract class AuthErrorCodes {
  static const invalidEmail = 'auth.invalidEmail';
  static const wrongPassword = 'auth.wrongPassword';
  static const userDisabled = 'auth.userDisabled';
  static const userNotFound = 'auth.userNotFound';
  static const credentialConflict = 'auth.credentialConflict';
  static const googleCancelled = 'auth.googleCancelled';
  static const generic = 'auth.generic';
}
